export type RantData = {
    rantID: string,
    userName: string,
    handle: string,
    title: string,
    body: string,
    likeCount: Number,
    commentCount: Number,
    createdAt: string,
    imageURL: string
}